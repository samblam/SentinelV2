# Core ML/CV Dependencies
torch>=2.1.0,<2.2.0
torchvision>=0.16.0,<0.17.0
numpy<2.0.0  # PyTorch 2.1.x requires numpy 1.x for .numpy()/.from_numpy() compatibility
ultralytics>=8.0.0,<9.0.0
opencv-python>=4.8.0
Pillow>=10.0.0

# YOLOv5 Runtime Dependencies
# Note: These are required at runtime, not optional
pandas>=2.0.0  # Required: inference.py uses results.pandas().xyxy[0]
tqdm>=4.65.0  # Required: YOLOv5 model loading and inference progress
seaborn>=0.12.0  # Required: YOLOv5 internal dependencies

# API Framework
fastapi>=0.111.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Configuration & Data
pydantic>=2.5.0
pydantic-settings>=2.1.0
aiosqlite>=0.19.0
